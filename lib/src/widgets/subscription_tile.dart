import 'package:flutter/material.dart';

class SusbscriptionTile extends StatelessWidget {
  const SusbscriptionTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      width: double.infinity,
      height: 100,
      color: Colors.red,
    );
  }
}
